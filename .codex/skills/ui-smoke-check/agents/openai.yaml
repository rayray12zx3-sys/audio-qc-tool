interface:
  display_name: "UI Smoke Check"
  short_description: "Check desktop UI states and interaction regressions."
  default_prompt: "Use $ui-smoke-check to verify desktop UI states and interaction regressions in this repo."

policy:
  allow_implicit_invocation: true
