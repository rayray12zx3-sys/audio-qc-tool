# AGENTS.md

## 專案摘要

- 這是一個音檔調音工具專案。
- 成品會以 GitHub 靜態網頁部署。
- 核心方向是前端操作與瀏覽器端音訊處理，不預設依賴後端。

## 目標

- 提供可直接上 GitHub Pages 的靜態站。
- 讓使用者能完成音檔匯入、播放、調整、預覽、匯出等操作。
- 介面要清楚、直覺、可在桌機與手機使用。
- 保持程式可維護，避免把商業邏輯和 UI 混在一起。

## 工作守則

- 先讀現有檔案、架構、設定，再開始動手。
- 以最小變更完成需求，避免順手重構無關區塊。
- 保留使用者與既有流程已做的修改，不要自行覆蓋。
- 遇到不確定處，先列出假設與風險，不要硬猜。
- 能驗證就驗證；不能驗證要明講原因。
- 使用繁體中文（台灣）回覆，技術名詞維持英文原文。

## 音檔處理原則

- 涉及音訊處理時，優先確認瀏覽器可行性與效能成本。
- 若使用 Web Audio API、Canvas、WebAssembly、Worker 或第三方音訊函式庫，要先確認是否仍能維持靜態部署。
- 音檔相關功能優先顧及：
  - 即時回饋
  - 播放穩定性
  - 狀態可視化
  - 錯誤提示清楚
  - 大檔案處理的效能
- 若計算量偏重，優先考慮：
  - 分批處理
  - Worker 背景執行
  - 降低預設負載
  - 提供處理進度與取消機制

## 靜態網站規範

- 以 GitHub Pages 可直接部署為前提設計。
- 不預設伺服器端 API。
- 若真的需要外部服務，要清楚標示依賴與風險。
- 路徑、base path、資產引用與 SPA 路由要特別注意，避免上 GitHub 後壞掉。
- build 輸出若有 `dist/`、`build/` 或類似資料夾，應以可發佈靜態檔案為目標。

## 開發節奏

- 新功能先找最接近的既有模組延伸。
- UI、音訊處理、匯出流程要分開思考，不要一次塞進單一檔案。
- 若有測試、lint、format、build 指令，修改後優先跑過。
- 若沒有自動測試，至少做人工驗證並說明驗證範圍。

## 任務狀態交接

- 每次開始工作時先讀取根目錄的 `TASK_STATE.md`，再依目前檔案與 Git 狀態核對內容是否仍有效。
- 任務目標、重要決策、修改範圍、驗證結果、風險或下一步有實質變化時，應同步更新 `TASK_STATE.md`。
- 在結束工作、切換對話或預期上下文可能被精簡前，必須更新 `TASK_STATE.md`，讓後續對話可直接接手。
- `TASK_STATE.md` 只保留目前有效狀態並維持精簡；歷史迭代仍記錄於 `docs/worklog.md`，不要把狀態檔寫成流水帳。
- 不得在 `TASK_STATE.md` 記錄密鑰、私人音檔名稱或其他不應提交至 Git 的資料。

## 回覆規則

- 先講結果，再補必要細節。
- 回覆保持精簡，避免空話與重複。
- 若有修改，結尾只保留必要資訊：
  - 完成事項
  - 修改檔案
  - 驗證方式
  - 注意事項

## UI 風格

- 以實用、清楚、可維護為優先。
- 不要用太制式的預設版型。
- 音檔工具介面要重視資訊層級、操作回饋與狀態顯示。
